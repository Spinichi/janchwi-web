spring:
  application:
    name: janchwi-backend
  config:
    import: optional:file:.env[.properties]
  profiles:
    default: dev

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO